import * as expoStatusBar from 'expo-status-bar';
import React from 'react';
import { Text, View } from 'react-native';

export default () => {
  return (
     <View>
      <Text>Feed the pigs</Text>
    </View>
  );
};
